# Chat HTML

Entre nesta pasta:

```powershell
cd app\examples\chat-html
```

Execute:

```powershell
docker compose up -d
```

Abra http://127.0.0.1:8088.

Para parar:

```powershell
docker compose down
```
